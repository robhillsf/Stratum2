#pragma once

#include <JuceHeader.h>
#include "PluginProcessor.h"
#include "UI/MainInterface.h"

//==============================================================================
class Stratum2AudioProcessorEditor : public juce::AudioProcessorEditor,
                                     public juce::Timer
{
public:
    Stratum2AudioProcessorEditor (Stratum2AudioProcessor&);
    ~Stratum2AudioProcessorEditor() override;

    //==============================================================================
    void paint (juce::Graphics&) override;
    void resized() override;

    //==============================================================================
    void timerCallback() override;

private:
    // This reference is provided as a quick way for your editor to
    // access the processor object that created it.
    Stratum2AudioProcessor& audioProcessor;

    // Main interface component
    std::unique_ptr<MainInterface> mainInterface;

    // Window size constraints
    static constexpr int minWidth = 1200;
    static constexpr int minHeight = 800;
    static constexpr int defaultWidth = 1400;
    static constexpr int defaultHeight = 900;

    JUCE_DECLARE_NON_COPYABLE_WITH_LEAK_DETECTOR (Stratum2AudioProcessorEditor)
};